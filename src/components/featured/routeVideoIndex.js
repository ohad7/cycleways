// Relocated to @cycleways/core/featured/routeVideoIndex.js (shared web + native,
// platform-agnostic via getJsonAsset). This re-export keeps existing web imports
// working.
export * from "@cycleways/core/featured/routeVideoIndex.js";
