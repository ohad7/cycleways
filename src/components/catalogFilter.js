// Moved into @cycleways/core/data/catalogFilter.js so the React Native app can
// reuse the same catalog filtering. Re-exported here for existing web imports.
export {
  catalogFilter,
  distanceBucketOf,
  DISTANCE_BUCKETS,
  DIFFICULTY_BUCKETS,
} from "@cycleways/core/data/catalogFilter.js";
