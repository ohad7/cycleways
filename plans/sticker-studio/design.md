# Cycleways Sticker Studio Design

**Date:** 2026-07-11

## Goal

Create a private, browser-based marketing tool that turns the Cycleways badge
artwork into printable stickers with an editable caption and an optional QR code
for the homepage, a recommended route, or another URL.

## Brand treatment

- Use `cycleways.app` without `www.`.
- Render `cycleways` at medium/semibold weight and `.app` at regular weight.
- Keep the wordmark smaller and less dominant than the rider and landscape.
- Preserve high-contrast white-on-charcoal printing; visual lightness comes from
  weight, scale, spacing, and a shallower-feeling line of type.
- Provide male and female rider badge templates. Text and QR modules are always
  generated by the tool rather than baked into a template image.

## Sticker composition

The first template family uses the 1024-by-1024 illustrated badge. A clean dark
banner covers the old baked lettering and receives the new wordmark. The lower
panel receives up to two caption lines. When enabled, the QR occupies a dedicated
white square over the central landscape.

QR codes use error-correction level Q, a four-module quiet zone, pure charcoal
modules, and a pure white background. The UI reports the physical module size at
the chosen sticker diameter and warns below 0.4 mm per module.

## User flow

1. Choose the rider template.
2. Choose homepage, route, or custom URL.
3. Enter one or two caption lines in Hebrew or English.
4. Choose printed size and export density.
5. Review the live sticker and QR print-safety status.
6. Download SVG or PNG, or open a repeated A4 print sheet.

## Architecture

The studio is a local Vite-served page under `marketing/sticker-studio/`. It is
not added to public navigation. Composition is deterministic SVG, with template
rasters embedded as data URLs before export. `qrcode-generator`, already used by
the application, supplies the QR matrix. Pure URL, caption, sizing, and print
metric helpers live in a small `.mjs` module so Node tests can cover them.

## Deferred decisions

- A true vector redraw and printer-specific contour cut line.
- A round-badge layout with an attached footer ribbon.
- Managed short-link redirects and per-placement scan analytics.
- Direct PDF generation; the MVP uses the browser's A4 print/PDF dialog.

